type GithubUser = {
        id: number;
        name: string;
        latitude: number;
        longitude: number;
    }

type Props = {
    user: GithubUser
};

export default function GithubUserMarker({ user } : Props)
{
    return (
        <
    );
}
